# Copy to .env and fill in real values (do not commit .env).
PROMQL_BASE_URL=https://prometheus.example.com
# Use one of the auth options below.
PROMQL_AUTH=user:password
# Or split basic auth into user/pass:
# PROMQL_USER=user
# PROMQL_PASS=password
# Or use bearer token:
# PROMQL_BEARER=token
